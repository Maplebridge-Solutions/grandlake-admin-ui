import PlaceholderPage from "@/components/placeholder";
import { Route } from "lucide-react";

export default function ManageRoutes() {
  return <PlaceholderPage title="Manage Routes" icon={Route} />;
}
